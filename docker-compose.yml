services:
  pwa-notes:
    container_name: pwa-notes
    build:
      context: .
      dockerfile: Dockerfile
    image: pwa-notes:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /opt/pwa/certs:/etc/nginx/certs:ro
    restart: unless-stopped
